## Python OpenUSD viewer

- Prerequisites: Have C++ compiler (Visual Studion in Windows, should be available by default in Linux)

1.- Create venv
2.- With venv open, build OpenUSD binaries

- Must have OpenUSD installed


Maya style controls: Press alt to move around with the mouse.